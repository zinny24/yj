<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>양정인력개발센터 게시물 목록 페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

    <div class="container">
        <h2 class="text-center mb-4">게시물 목록</h2>
        <!-- 검색 폼-->
        <div class="card p-4">
            <form action="/board/list" method="get" class="search-bar">
                <div class="search-box">
                    <select name="searchType" class="form-select">
                        <option value="subject">제목</option>
                        <option value="writer">작성자</option>
                    </select>
                    <input type="text" name="word" class="form-control" placeholder="검색어 입력">
                    <button type="submit" class="btn btn-search mt-2">검색</button>
                </div>
            </form>
        </div>
        <!-- //검색 폼-->
        <div class="card mt-4">
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>번호</th>
                            <th>제목</th>
                            <th>작성자</th>
                            <th>작성일</th>
                            <th>비고</th>
                        </tr>
                    </thead>
                    <tbody>
                    <!-- controller data 반복문 -->
                    {{#list}}
                        <tr>
                            <td>{{id}}</td>
                            <td>
                                <strong>
                                    <a href="/board/view/{{id}}">{{subject}}</a>
                                </strong>
                            </td>
                            <td>{{writer}}</td>
                            <td>{{createDate}}</td>
                            <td>
                                <a href="/board/edit/{{id}}">수정</a>
                                <a href="/board/delete/{{id}}">삭제</a>
                            </td>
                        </tr>
                        {{/list}}
                    <!-- //controller data 반복문 -->
                    </tbody>
                </table>
            </div>
            <a href="/board/create" class="btn btn-info">게시물 작성</a>
        </div>

    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>