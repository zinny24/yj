<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>사원 목록</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Noto Sans KR", sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        background-color: #f5f5f5;
      }
      p {
        font-size: 18px;
        font-weight: 700;
        margin-bottom: 10px;
        text-align: left;
        width: calc(100% - 40px);
        margin-left: 20px;
      }
      table {
        width: calc(100% - 40px);
        margin: 0 20px;
        border-collapse: collapse;
        background: #fff;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      th,
      td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: center;
        height: 30px;
      }
      th {
        background-color: #e3effb; /* 세련된 연한 회색빛 파랑 */
        color: #333;
        font-weight: 700;
      }
      td:nth-child(1),
      td:nth-child(3),
      td:nth-child(4),
      td:nth-child(5) {
        width: 100px;
      }
      td:nth-child(6) {
        width: 200px; /* 비고란 너비 증가 */
      }
      .footer {
        display: flex;
        justify-content: space-between;
        width: calc(100% - 40px);
        margin: 15px 0;
      }
      .footer .left,
      .footer .center,
      .footer .right {
        display: flex;
        align-items: center;
      }
      .footer .left button,
      .footer .center button,
      .footer .right button {
        padding: 10px 16px;
        font-size: 14px;
        cursor: pointer;
        border: 0px solid #ddd;
        background-color: #000;
        color: white;
        margin: 0 1px;
        transition: background-color 0.3s ease;
        border-radius: 3px;
      }
      .footer .left button:hover,
      .footer .center button:hover,
      .footer .right button:hover {
        background-color: #495057;
      }
      .action-buttons button {
        padding: 7px 14px; /* 크기 조정 */
        font-size: 14px; /* 글자 크기 16px로 변경 */
        border-radius: 2px; /* 버튼 모서리 라운드 처리 */
        cursor: pointer;
        border: none; /* 테두리 제거 */
        transition: background-color 0.3s ease;
      }
      .action-buttons .view {
        background-color: #007bff; /* 보기 버튼: 청록색 */
        color: white;
      }
      .action-buttons .view:hover {
        background-color: #138496; /* 보기 버튼 hover: 어두운 청록색 */
      }
      .action-buttons .edit {
        background-color: #ffc107; /* 수정 버튼: 노란색 */
        color: #fff;
      }
      .action-buttons .edit:hover {
        background-color: #e0a800; /* 수정 버튼 hover: 어두운 노란색 */
      }
      .action-buttons .delete {
        background-color: #dc3545; /* 삭제 버튼: 주황색 */
        color: white;
      }
      .action-buttons .delete:hover {
        background-color: #e46209; /* 삭제 버튼 hover: 어두운 주황색 */
      }
      .search-container {
        display: flex;
        align-items: center;
      }
      .search-container select {
        width: 100px; /* 검색어 입력 박스의 너비 */
        padding: 11px 4px;
        font-size: 14px;
        margin-right: 2px;
        border: 1px solid #ccc;
        border-radius: 3px;
        line-height: 24px; /* 세로 가운데 정렬을 위한 줄 간격 */
      }
      .search-container input {
        width: 150px; /* 검색어 입력 박스의 너비 */
        padding: 8px 7px;
        font-size: 14px;
        margin-right: 2px;
        border: 1px solid #ccc;
        border-radius: 3px;
        line-height: 24px; /* 세로 가운데 정렬을 위한 줄 간격 */
      }
    </style>
  </head>
  <body>
    <p>• 게시물 목록을 출력합니다</p>
    <table>
      <thead>
        <tr>
          <th>번호</th>
          <th>제목</th>
          <th>작성자</th>
          <th>조회수</th>
          <th>작성일</th>
          <th>비고</th>
        </tr>
      </thead>
      <tbody>
      {{#aList}}
        <tr>
          <td>{{id}}</td>
          <td style="text-align:left"><strong><a href="/articles/view/{{id}}">{{subject}}</a></strong></td>
          <td>{{writer}}</td>
          <td>{{viewCount}}</td>
          <td>{{createDate}}</td>
          <td class="action-buttons">
              <a href="/articles/view/{{id}}"><button class="view">보기</button></a>
              <a href="/articles/edit/{{id}}"><button class="edit">수정</button></a>
              <a href="/articles/delete/{{id}}" onclick="confirmDelete(event)"><button class="delete">삭제</button></a>
          </td>
        </tr>
        {{/aList}}
      </tbody>
    </table>

    <!-- Footer div with buttons -->
    <div class="footer">
      <!-- Left: Page number buttons -->
      <div class="left">
          <span>Page 1 of 0</span>
      </div>
      <div class="center search-container">

          <form action="/articles" method="get">
              <select name="searchType">
                  <option value="subject" {{#isSubject}} selected  {{/isSubject}}>제목</option>
                  <option value="writer" {{#isWriter}} selected  {{/isWriter}}>작성자</option>
              </select>
              <input value="{{searchWords}}" name="searchWords" type="text" placeholder="검색어" required />
              <button type="submit">검색</button>
          </form>
          
          

      </div>
      <div class="right">
          <a href="/articles/write"><button>게시물 작성</button></a>
          <a href="/articles"><button style="background-color: #ffc107">전체보기</button></a>
      </div>
    </div>


    <script>
        function confirmDelete(event) {
            if (!confirm("정말로 삭제하시겠습니까?")) {
                event.preventDefault();
            }
        }
    </script>
  </body>
</html>
